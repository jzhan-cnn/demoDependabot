pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
        maven {
            url = uri('https://storage.googleapis.com/r8-releases/raw')
        }
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        google()
        mavenCentral()
        maven { url = 'https://repo1.maven.org/maven2' }
        maven { url = 'https://plugins.gradle.org/m2/' }
        maven { url = 'https://maven.google.com' }

        // TOP 2
        maven {
            url = uri('https://soarin.jfrog.io/soarin/top-android-prod/')
            credentials {
                username = 'top-consumer'
                password = 'top-consumer-pass'
            }
        }
        maven {
            url = 'https://muxinc.jfrog.io/artifactory/default-maven-release-local'
        }
        maven {
            url = 'https://artifacts.bitmovin.com/artifactory/public-releases'
        }
        maven {
            url = 'https://s3.amazonaws.com/android.truex.com/maven'
        }
        maven {
            url = 'https://cherry-repo.com/repository/releases/'
        }
        // END TOP 2

        maven { url = 'https://github.com/yieldmo/maven-repository/raw/master/' }
        maven { url = 'https://oss.sonatype.org/content/repositories/snapshots/' }
        maven {
            url = 'https://www.jitpack.io'
            credentials {
                username = 'jp_dsp2esl43ugn7h71gv3n9oim47'
            }
        }
        maven {
            url = uri('https://api.mapbox.com/downloads/v2/releases/maven')
            credentials.username = 'mapbox'
            credentials.password = System.getenv('MAPBOX_DOWNLOADS_TOKEN')
            authentication { basic(BasicAuthentication) }
        }

        // PSDK
        maven {
            url = 'https://discovery.jfrog.io/artifactory/shared-remotes-maven-virtual'
            credentials {
                username = System.getenv('JFROG_USERNAME')
                password = System.getenv('JFROG_TOKEN')
            }
            mavenContent {
                snapshotsOnly()
            }
        }
        maven {
            url = 'https://discovery.jfrog.io/artifactory/shared-remotes-maven-virtual'
            credentials {
                username = System.getenv('JFROG_USERNAME')
                password = System.getenv('JFROG_TOKEN')
            }
            mavenContent {
                releasesOnly()
            }
        }
        maven {
            url = uri('https://discovery.jfrog.io/artifactory/wbd-kotlin')
            credentials {
                username = System.getenv('JFROG_USERNAME')
                password = System.getenv('JFROG_TOKEN')
            }
        }
        maven {
            url = uri('https://muxinc.jfrog.io/artifactory/default-maven-virtual')
        }
        maven {
            url = uri('https://muxinc.jfrog.io/artifactory/default-maven-local')
        }
        maven {
            url = uri('https://discovery.jfrog.io/artifactory/cd-android-discovery-player-maven-snapshots')
            credentials {
                username = System.getenv('JFROG_USERNAME')
                password = System.getenv('JFROG_TOKEN')
            }
            mavenContent {
                snapshotsOnly()
            }
        }
        maven {
            url = uri('https://discovery.jfrog.io/artifactory/cd-android-discovery-player-maven-releases')
            credentials {
                username = System.getenv('JFROG_USERNAME')
                password = System.getenv('JFROG_TOKEN')
            }
            mavenContent {
                releasesOnly()
            }
        }
        // END PSDK
    }
}
include ':main',
        ':arkose-sdk-2.4.1'
